{
  "name": "ts-http",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -r dist || true && tsc"
  },
  "type": "module",
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.0",
    "@types/node": "^18.0.0",
    "prettier": "2.8.7",
    "typescript": "^5.0.0"
  }
}
